#!/usr/bin/env bash

stow --target="$HOME" nvim -R
stow --target="$HOME" bin -R
stow --target="$HOME" zsh -R
stow --target="$HOME" tmux -R
stow --target="$HOME" alacritty -R
stow --target="$HOME" i3 -R
